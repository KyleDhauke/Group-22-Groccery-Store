// Useful venv commands //

Make sure that you're in the correct directory before running these!

+-------------------------------------+

py -m venv venv

venv\Scripts\activate

py -m pip install -r requirements.txt

set FLASK_DEBUG="1"

set FLASK_APP= app

flask run

+-------------------------------------+